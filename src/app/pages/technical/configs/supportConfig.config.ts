import { MatDialogConfig } from "@angular/material/dialog";

const supportConfig = new MatDialogConfig();

supportConfig.width = "500px";
supportConfig.height = "300px";

export default supportConfig;