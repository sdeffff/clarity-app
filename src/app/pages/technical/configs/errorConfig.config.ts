import { MatDialogConfig } from "@angular/material/dialog";

const errorConfig = new MatDialogConfig();

errorConfig.width = "400px";
errorConfig.height = "150px";
errorConfig.position = {
    top: "2rem",
};

export default errorConfig;